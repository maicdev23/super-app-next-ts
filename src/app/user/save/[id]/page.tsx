export { default } from '../page'
/*import { getUser } from "../../user.service"

const UserId = async ({ params }: any) => {

    const user = await getUser(params.id)
    console.log(user)
    return (
        <>
            <div>Holis {params.id}</div>
            <div className="card">
               <p>{user.name}</p> 
            </div>
        </>
    )
}

export default UserId*/